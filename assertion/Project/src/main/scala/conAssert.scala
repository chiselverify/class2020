object conAssert {
    /** Checks for a condition to be valid in the circuit concurrently with time.
      * If the condition evaluates to false, the test stops with an error
      * 
      * @param cond condition, assertion fires (simulation fails) when false
      * @param cycles number of clock-cycles the assertions checks condition for
      * @param message optional format string to print when the assertion fires
      * @param data optional bits to print in the message formatting
      * 
      * @note This object is part 
      *
      *
      *
      *
      *
      
    */
}